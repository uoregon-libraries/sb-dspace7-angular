import { ResourceType } from '../../shared/resource-type';

/**
 * The resource type for Contactus
 *
 * Needs to be in a separate file to prevent circular
 * dependencies in webpack.
 */
export const CONTACTUS = new ResourceType('contactus');
